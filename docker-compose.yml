version: "3.8"

services:
  backend:
    build: ./backend
    container_name: stack-practice-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - sqlite_data:/app/data
    command: uvicorn app.app:app --host 0.0.0.0 --port 8000 --reload

volumes:
  sqlite_data:
